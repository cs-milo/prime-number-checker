# prime-number-checker
Checks if a given number is prime within a custom range provided by the user.

---

## 📁 `PrimeNumberChecker.js` — Prime Number Checker with Custom Range

# Prime Number Checker with Custom Range

This JavaScript program allows the user to enter a range of numbers and checks for all the prime numbers within that range.

## 📌 Features
- Prompts the user to enter a start and end number.
- Validates input to ensure it’s a valid positive range.
- Uses a prime-checking function optimized with square root logic.
- Displays all prime numbers in the specified range.

## 🛠 How to Run

1. Install [Node.js](https://nodejs.org/) if you haven't already.
2. Open a terminal or Command Prompt.
3. Navigate to the folder containing `PrimeNumberChecker.js`.
4. Run the program using:

   ```bash
   node PrimeNumberChecker.js
